package com.klebermagno.code.cap3;

/**
 * Sort Stack: Write a program to sort a stack such that
 * the smallest items are on the top.
 * You can use an additional temporary stack, but you
 * may not copy the elements into any other data structure
 * (such as an array).
 * The stack supports the following operations: push, pop, peek, and isEmpty.
 */

/**
 * Classificar pilha: Escreva um programa para classificar uma pilha de modo
 * que os menores itens fiquem no topo.
 * Você pode usar uma pilha temporária adicional,
 * mas não pode copiar os elementos em nenhuma outra estrutura de dados (como uma matriz).
 * A pilha suporta as seguintes operações: push, pop, peek e isEmpty.
 * FOLLOW UP
 * Implemente uma função popAt(int index) que executa uma operação pop em uma subpilha específica.
 */
public class Q35 {
}
