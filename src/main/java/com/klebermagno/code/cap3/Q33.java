package com.klebermagno.code.cap3;

/**
 * Stack of Plates: Imagine a (literal) stack of plates.
 * If the stack gets too high, it might topple.Therefore,
 * in real life, we would likely start a new stack when
 * the previous stack exceeds some threshold. Implement
 * a data structure SetOfStacks that mimics this. SetO-fStacks
 * should be composed of several stacks and should create a new
 * stack once the previous one exceeds capacity. SetOfStacks.
 * push() and SetOfStacks. pop() should behave identically to
 * a single stack (that is, pop() should return the same
 * values as it would if there were just a single stack).
 * FOLLOW UP
 * Implement a function popAt(int index) which performs a pop operation on a specific sub-stack.
 */

/**
 * Pilha de Pratos: Imagine uma pilha (literal) de pratos.
 * Se a pilha ficar muito alta, ela pode tombar.
 * Portanto, na vida real, provavelmente iniciaríamos uma nova pilha
 * quando a pilha anterior exceder algum limite.
 * Implemente uma estrutura de dados SetOfStacks que imita isso.
 * SetO-fStacks deve ser composto por várias pilhas e
 * deve criar uma nova pilha assim que a anterior exceder a capacidade.
 * SetOfStacks. push() e SetOfStacks. pop() deve se comportar de
 * forma idêntica a uma única pilha (ou seja, pop() deve retornar
 * os mesmos valores que se houvesse apenas uma única pilha).
 * ACOMPANHAMENTO
 * Implemente uma função popAt(int index) que executa uma operação pop em uma subpilha específica.
 */
public class Q33 {
}
